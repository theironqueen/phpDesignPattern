按照书上的代码往下敲，这种编写项目的架构跟使用thinkphp等框架的格式很像，但在编写的时候还是感觉到了很多的不习惯。从前写的多是直接用html写出样式，然后将数据通过ajax获取，用js填写到页面上，而不是使用php在后台获取数据直接生成相应的页面。书上写的代码理解起来不困难，也都清楚的明白各个类之间的作用方式是什么样的，整个项目是如何跑起来的，但是个人来说不太习惯这样写。或许这种方式才是面向对象的思考方式，之前的写法都太过过程化，所有的作业方式都是一步步的来，思考方式也都是按一个流程来写的。